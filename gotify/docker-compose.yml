version: "3.9"

services:
  gotify_db:
    image: docker.io/library/postgres:15.17-alpine@sha256:fceb6f86328c36f2438fae3b851b0cc57c4a7e69a58c866d9ce24281f2cf0c9c
    restart: unless-stopped
    container_name: gotify_db
    # See: https://github.com/docker-library/docs/blob/master/postgres/README.md#arbitrary---user-notes
    user: "${PUID}:${PGID}"
    volumes:
      - ./db:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=gotify
      - POSTGRES_USER=gotify
      - POSTGRES_PASSWORD=${GOTIFY_DB_PASSWORD}
    networks:
      - gotify
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}"]
      start_period: 20s
      interval: 30s
      retries: 5
      timeout: 5s

  gotify_db_dumper:
    image: docker.io/paolobasso/database_dumper:postgres-15
    restart: unless-stopped
    depends_on:
      - gotify_db
    container_name: gotify_db_dumper
    volumes:
      - ./db_dumps:/dumps
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - DUMPER_DATABASE=gotify
      - DUMPER_HOST=gotify_db
      - DUMPER_USER=gotify
      - DUMPER_PASSWORD=${GOTIFY_DB_PASSWORD}
      - DUMPER_HEALTHCHECKS_URL=${GOTIFY_DB_DUMPER_HEALTHCHECKS_URL}
    networks:
      - gotify

  gotify:
    image: docker.io/gotify/server:2.9.1@sha256:a3af47067ce6aad76aadf5ba32d6ddfecd1ae576a961359f039fd1831e8b7652
    depends_on:
      - gotify_db
    restart: unless-stopped
    container_name: gotify
    user: "${PUID}:${PGID}"
    environment:
      - GOTIFY_DATABASE_DIALECT=postgres
      - GOTIFY_DATABASE_CONNECTION=host=gotify_db port=5432 user=gotify dbname=gotify password=${GOTIFY_DB_PASSWORD} sslmode=disable
    labels:
      - "traefik.enable=true"
      - "traefik.http.services.gotify.loadbalancer.server.port=80"
      - "traefik.http.routers.gotify.service=gotify"
      - "traefik.http.routers.gotify.tls=true"
      - "traefik.http.routers.gotify.entrypoints=websecure"
      - 'traefik.http.routers.gotify.rule=Host("$GOTIFY_HOST")'
    volumes:
      - ./data:/app/data
    networks:
      - gotify
      - web_proxy
    # No healthcheck because already present in Dockerfile

networks:
  web_proxy:
    external: true
  gotify:
    driver: bridge
