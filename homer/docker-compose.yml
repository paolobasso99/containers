version: "3.9"

services:
  homer:
    image: docker.io/b4bz/homer:v25.03.2@sha256:24fa81e40e01d9d2fbeb187a1a7e561a62494bd56243156ab88cefe513111921
    container_name: homer
    volumes:
      - ./assets:/www/assets
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.services.homer.loadbalancer.server.port=8080"
      - "traefik.http.routers.homer.service=homer"
      - "traefik.http.routers.homer.tls=true"
      - "traefik.http.routers.homer.entrypoints=websecure"
      - 'traefik.http.routers.homer.rule=Host("$HOMER_HOST")'
      - "traefik.http.routers.homer.middlewares=authelia@docker"
    networks:
      - web_proxy
    # No healthchek because already present in Dockerfile

networks:
  web_proxy:
    external: true
